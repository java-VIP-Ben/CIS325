# Benjamin Lukens CIS325 Midterm project Readme file for project2

To run this app

make sure npm is installed with:
npm install

then use npm to run the program:
npm run dev

This will return a localhost link and a port address. Copy and paste it into your browser
to view the webpage. 
